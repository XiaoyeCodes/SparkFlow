# IBKR 软件缺口与外部检查点

2026-09-05 恢复审计。主计划第 12 节不变；先前进度中 VERIFIED 只适用于记录过的测试行为，不能推导为整个阶段完成。所有已存在用户改动保留，不重新执行已通过且未受修改影响的验证。

| 优先级/状态 | 软件缺口与源码依据 | 可继续的授权内工作 | 真正的外部检查点 |
| --- | --- | --- | --- |
| P3.2h/i VERIFIED（离线） | 原始回报 observer 已接到用户配置的人工 paper flow；未配置时不创建写适配 | UNKNOWN 归属、成交/手续费/更正测试保留；仍需完整生产账户证明、现金流映射与错误后的显式恢复核对 | 实际 paper 回调与账单核对 |
| P3.3a/b 新单与撤单 VERIFIED（离线）/ 改单 TODO | NativeDispatcher 原生新单、独立撤单许可、超时与恢复已测试；manual paper API/UI 按用户限额与逐笔确认创建适配；默认停用 | 下一步原生改单；当前生产风险来源拒绝任何已有挂单，尚需剩余风险映射；终态预占释放需账户证明接通，不能手动抹账 | 指定真实 paper 账户、品种、测试限额/期限；实时逐笔行情、PnL、常规交易时段；Gateway 写权限由用户设置 |
| P4 TODO | 页面策略编辑器只导出草稿；生产回测没有可信数据导入/提交操作链 | 本地不可变版本保存、受验证数据导入和受限任务启动 | 用户策略规则/版本；历史数据及使用权限 |
| P5 TODO | runtime 只生成 PERSISTED 意图，未接执行适配；生产没有激活/恢复操作链 | 授权与激活分离的统一执行、恢复先对账及启停控制 | 用户策略/限额/期限；两个有效 paper 时段 |
| P6 TODO | intelligence.py 只构建上下文；无模型调用器或用户分享管理；报告任务未提供显式重试 | 复用现有模型传输，在发送时复查独立同意与预算；取消/撤销/注入/失败测试；报告重试 | 模型服务、允许字段、预算、期限；实际数据/微观权限 |
| P7 TODO / 外部 BLOCKED | live 默认拒绝已验证，不等于可授权启用链已实现 | 与人工确认分离的授权状态、失效即时检查和离线拒绝回归 | live 查看配置；真实写入另行完整明确授权 |
| P8 后续复核 | 原冻结前端 60 分钟数据有效，只证明对应构建；后续行为变化需相应回归 | 对每次变更做有针对性验证，最终再冻结相关构建 | 真实连接的持续观察条件 |

P3 的 SDK 回报语义参照固定本机 ib_async 2.1.0 源码和 [IBKR Execution 文档](https://www.interactivebrokers.com/docs/tws-api/doc/order-management/execution-details/the-execution-object)。更正只按明确执行身份建立链；缺少前序不能猜测。未获得原始字段时不能把 SDK 对象默认值当成券商事实。

2026-09-06：实际 paper 账户 DU***372 已通过只读连接并返回完整当前持仓/订单/成交查询；尚未获得具体测试订单范围，没有发送任何订单。人工风险范围由用户在订单工作区填写，启动/重启默认关闭，停止不会撤单或平仓，重新配置暂需重启服务。live 写入、自动策略及 AI 分享仍关闭。本文件不是授权记录。

本轮 UI 有变更，原冻结版本的 60 分钟性能结果不能作为新构建的长测验收；新版长测未运行。
