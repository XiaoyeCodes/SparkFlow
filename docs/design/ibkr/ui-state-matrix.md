# IBKR 终端分区状态矩阵

更新：2026-09-05。矩阵描述生产界面实际文案与拒绝行为；工程 fixture 仅用于自动化验证。

| 区域 | loading | empty | error | stale | permission-required |
| --- | --- | --- | --- | --- | --- |
| 顶栏／账户概览 | “正在同步账户”；金额为 `—` | “账户暂无持仓” | “账户同步失败，请检查本地服务” | 保留数值并显示“数据已过期／等待重新对账” | “账户读取权限不足” |
| 中栏历史图表 | “正在读取历史行情” | “所选区间无行情” | 显示请求或契约错误 | 有 bars 时标“缓存已过期”，无 bars 时显示过期空态 | “历史行情权限未配置”，保留缺失代码 |
| 中栏持仓／挂单／成交 | “正在同步…” | 各表明确“尚无已同步记录” | “同步失败” | “保留的数据已过期” | “缺少读取权限” |
| 订单工作区 | 禁用表单，等待同步 | 无可用合约时禁用 | 同步失败时拒绝新增风险 | 明确“账户快照已过期，拒绝新增风险” | 缺账户或行情权限时拒绝新增风险 |
| 策略／回测 | “正在读取本地回测目录” | 无用户策略、生产数据、任务或结果均独立空态 | 回测／运行服务错误独立显示 | 账户过期时不加载运行实例，恢复要求重新对账 | 缺生产数据或策略授权时禁用运行；live 无启动入口 |
| 账户风险／报告／AI | 风险指标与报告任务分别显示进行中 | 不生成固定分数；无报告保持空 | 本地分析／任务错误可见 | 账户过期后清空分析并显示“已暂停分析” | AI 分享关闭；未授权时输入、模型调用与账户外发均禁用 |
| 新闻 | 独立显示“正在读取新闻证据” | “没有匹配的可验证新闻” | 新闻源错误独立显示，不影响账户或图表 | 每条证据显示“陈旧”及发布／抓取时间 | 来源拒绝访问时显示返回的权限错误，不填充演示新闻 |
| 宏观 | 独立显示“正在读取宏观证据” | 无项目时保持证据空态 | 宏观源错误独立显示 | `delayed` 来源明确标记并显示数据时间 | 无来源或不可用来源标为“来源不可用”，不映射账户结论 |
| 微观 | 不与其他区域共用加载状态 | 无来源时显示固定空态 | 来源接入后沿用独立错误 | 接入后要求显示来源时间／过期状态 | 当前明确说明资金流、期权异动、机构持仓与评级需要各自权限 |

验证入口：`tests/ibkr/terminal.spec.ts` 覆盖生产空态、账户过期、历史行情权限、服务错误、独立资讯、AI 关闭、模式切换和晚到响应隔离。状态语义源自 `Snapshot.state`、历史行情 `state`、报告／回测任务状态，以及新闻／宏观来源证据，不从缓存年龄或缺失数字猜测“正常”。
